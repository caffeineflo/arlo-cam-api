FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir .

COPY src/ ./src/
COPY config.yaml ./config.yaml

EXPOSE 4000/tcp 4100/tcp 5000/tcp

CMD ["python", "-m", "src.main"]
